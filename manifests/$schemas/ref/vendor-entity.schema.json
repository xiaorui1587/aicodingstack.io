{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vendor-entity.schema.json",
  "title": "Vendor Entity Schema",
  "description": "Extended entity schema with vendor-related fields for models, providers, and MCP servers",
  "type": "object",
  "allOf": [
    {
      "$ref": "./entity.schema.json"
    },

    {
      "type": "object",
      "properties": {
        "docsUrl": {
          "type": ["string", "null"],
          "format": "uri",
          "pattern": "^https://",
          "description": "Documentation URL, usually the official website documentation page (typical formats include docs.example.com or example.com/docs)"
        },
        "vendor": {
          "type": "string",
          "description": "The company or organization that develops this entity"
        }
      },
      "required": ["docsUrl", "vendor"]
    }
  ]
}
