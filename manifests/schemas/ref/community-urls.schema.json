{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Community URLs Schema",
  "description": "Schema for community presence URLs",
  "type": "object",
  "properties": {
    "linkedin": {
      "type": ["string", "null"],
      "format": "uri",
      "pattern": "^https://",
      "description": "LinkedIn company page URL"
    },
    "twitter": {
      "type": ["string", "null"],
      "format": "uri",
      "pattern": "^https://",
      "description": "Twitter/X profile URL"
    },
    "github": {
      "type": ["string", "null"],
      "format": "uri",
      "pattern": "^https://",
      "description": "GitHub organization URL"
    },
    "youtube": {
      "type": ["string", "null"],
      "format": "uri",
      "pattern": "^https://",
      "description": "YouTube channel URL"
    },
    "discord": {
      "type": ["string", "null"],
      "format": "uri",
      "pattern": "^https://",
      "description": "Discord server URL"
    },
    "reddit": {
      "type": ["string", "null"],
      "format": "uri",
      "pattern": "^https://",
      "description": "Reddit community URL"
    },
    "blog": {
      "type": ["string", "null"],
      "format": "uri",
      "pattern": "^https://",
      "description": "Official blog URL"
    }
  },
  "required": ["linkedin", "twitter", "github", "youtube", "discord", "reddit", "blog"],
  "additionalProperties": false
}

