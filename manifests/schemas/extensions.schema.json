{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Extensions Manifest Schema",
  "description": "Schema for IDE extensions metadata",
  "type": "array",
  "items": {
    "type": "object",
    "allOf": [
      { "$ref": "./ref/product.schema.json" },
      {
        "type": "object",
        "properties": {
          "supportedIdes": {
            "type": "array",
            "description": "List of IDEs that support this extension",
            "items": {
              "$ref": "#/$defs/ideSupport"
            },
            "minItems": 1,
            "uniqueItems": true
          }
        },
        "required": ["supportedIdes"]
      }
    ]
  },
  "$defs": {
    "ideSupport": {
      "type": "object",
      "description": "IDE-specific extension support information",
      "properties": {
        "ideId": {
          "type": "string",
          "enum": ["vscode", "jetbrains", "cursor", "windsurf", "trae", "zed"],
          "description": "IDE identifier (e.g., 'vscode', 'jetbrains')"
        },
        "marketplaceUrl": {
          "type": ["string", "null"],
          "format": "uri",
          "pattern": "^https://",
          "description": "Extension marketplace URL for this IDE"
        },
        "installUri": {
          "type": ["string", "null"],
          "format": "uri",
          "description": "Deep link URI for one-click installation"
        }
      },
      "required": ["ideId", "marketplaceUrl", "installUri"],
      "additionalProperties": false
    }
  }
}
